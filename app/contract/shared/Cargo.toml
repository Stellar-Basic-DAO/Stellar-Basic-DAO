[package]
name = "stellar-dao-shared"
version = "0.1.0"
edition = "2021"
description = "Shared types, errors, and utilities for Stellar Basic DAO sub-contracts"
license = "MIT OR Apache-2.0"

[lib]

[dependencies]
soroban-sdk = "23"
