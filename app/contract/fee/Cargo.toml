[package]
name = "stellar-dao-fee"
version = "0.1.0"
edition = "2021"
description = "Stellar Basic DAO - Fee Router Sub-contract"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "23"
stellar-dao-shared = { path = "../shared" }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
